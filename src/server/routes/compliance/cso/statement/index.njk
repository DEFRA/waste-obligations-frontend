{% extends 'compliance/compliance-about.njk' %}

{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "component-locale/macro.njk" import componentT %}

{% set localeBase = 'compliance.statement' %}

{% block aboutLead %}
  <p class="govuk-body">
    {{ componentT(locale, localeBase, 'about', 'introPrefix') }}
    {{ appLinkOpensInNewWindow({
      href: regulation43Url,
      text: t(locale, "common.regulation43Text"),
      hiddenText: t(locale, "common.opensInNewTab")
    }) }}
    {{ componentT(locale, localeBase, 'about', 'introSuffix') }}
  </p>

  <ul class="govuk-list govuk-list--bullet">
    <li>{{ componentT(locale, localeBase, 'about', 'bullet1') }}</li>
    <li>{{ componentT(locale, localeBase, 'about', 'bullet2') }}</li>
    <li>{{ componentT(locale, localeBase, 'about', 'bullet3') }}</li>
  </ul>

  <p class="govuk-body">
    {{ componentT(locale, localeBase, 'about', 'description1', { year: year, regulatorName: regulatorName }) }}
  </p>
{% endblock %}

{% block mustBullets %}
  <li>{{ componentT(locale, localeBase, 'about', 'mustBullet1') }}</li>
  <li>{{ componentT(locale, localeBase, 'about', 'mustBullet2') }}</li>
  <li>{{ componentT(locale, localeBase, 'about', 'mustBullet3') }}</li>
{% endblock %}

{% block howToBullets %}
  <li>{{ componentT(locale, localeBase, 'about', 'howToBullet1') }}</li>
  <li>{{ componentT(locale, localeBase, 'about', 'howToBullet2') }}</li>
  <li>{{ componentT(locale, localeBase, 'about', 'howToBullet3') }}</li>
  <li>{{ componentT(locale, localeBase, 'about', 'howToBulletFullName') }}</li>
{% endblock %}

{% block finalStatus %}
  <p class="govuk-body">{{ componentT(locale, localeBase, 'about', 'finalStatusIntro') }}</p>

  <ul class="govuk-list govuk-list--bullet">
    <li>{{ componentT(locale, localeBase, 'about', 'finalStatusBullet1') }}</li>
    <li>{{ componentT(locale, localeBase, 'about', 'finalStatusBullet2') }}</li>
  </ul>
{% endblock %}

{% block aboutActions %}
  {% if showContinueToSubmit %}
  {{ govukButton({
    text: t(locale, "common.continue"),
    href: submitHref
  }) }}
  {% else %}
  <p class="govuk-body">
    {{ componentT(locale, localeBase, 'about', 'alreadySubmittedLead', { year: year }) }}
  </p>
  {% endif %}
{% endblock %}
