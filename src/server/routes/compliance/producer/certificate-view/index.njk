{% extends 'layouts/page.njk' %}

{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "certificate-obligations-table/macro.njk" import appCertificateObligationsTable %}
{% from "compliance-submit/declaration-text/macro.njk" import appComplianceSubmitDeclarationText %}
{% from "component-locale/macro.njk" import componentLocaleKey, componentT %}

{% set pageLocaleBase = 'compliance.certificateView' %}
{% set declarationLocaleBase = 'compliance.certificateSubmit' %}
{% set heading = t(locale, pageLocaleBase + ".heading", { year: year }) %}
{% set pageTitle = heading %}

{% block content %}
  <div class="app-certificate">
    {{ appHeading({
      caption: componentT(locale, pageLocaleBase, 'page', 'preHeader'),
      captionSize: "xl",
      text: heading,
      headingSize: "xl",
      gridColumnClasses: "govuk-grid-column-full"
    }) }}

    <section class="govuk-grid-row" aria-labelledby="organisation-summary-heading">
      <div class="govuk-grid-column-two-thirds">
        {{ govukSummaryList({
          classes: "govuk-!-margin-bottom-0",
          rows: [
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'organisationName'), attributes: { id: "organisation-summary-heading" } },
              value: { text: organisationName }
            },
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'organisationId') },
              value: { text: organisationNumber }
            },
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'address') },
              value: { text: organisationAddress }
            },
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'nameOnAccount') },
              value: { text: nameOnAccount }
            },
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'submissionDate') },
              value: { text: submissionDate }
            },
            {
              key: { text: componentT(locale, pageLocaleBase, 'summaryList', 'regulator') },
              value: { text: regulatorName }
            }
          ]
        }) }}
      </div>
    </section>

    <section class="govuk-grid-row" aria-labelledby="overall-status-heading">
      <div class="govuk-grid-column-full">
        <h2 id="overall-status-heading" class="govuk-heading-l">{{ componentT(locale, pageLocaleBase, 'overallStatus', 'recyclingObligationsStatusHeading') }}</h2>
        {% if obligationStatus == 'Met' %}
          <div class="app-obligation-status app-obligation-status--met govuk-!-margin-bottom-6" role="status">
            <p class="app-obligation-status__text govuk-body-l govuk-!-font-weight-bold">
              {{ componentT(locale, pageLocaleBase, 'overallStatus', 'overallMet') }}
            </p>
          </div>
        {% else %}
          <div class="app-obligation-status app-obligation-status--not-met govuk-!-margin-bottom-6" role="status">
            <p class="app-obligation-status__text govuk-body-l govuk-!-font-weight-bold">
              {{ componentT(locale, pageLocaleBase, 'overallStatus', 'overallNotMet') }}
            </p>
          </div>
        {% endif %}
      </div>
    </section>

    <section class="govuk-grid-row" aria-labelledby="obligations-table-heading">
      <div class="govuk-grid-column-full">
        <h2 id="obligations-table-heading" class="govuk-heading-l">{{ componentT(locale, pageLocaleBase, 'obligationsTable', 'recyclingObligationsHeading') }}</h2>
        <p id="obligations-tonnes-note" class="govuk-body govuk-!-margin-bottom-4">{{ componentT(locale, pageLocaleBase, 'obligationsTable', 'tonnesNote') }}</p>

        {{ appCertificateObligationsTable({
          locale: locale,
          tableRows: obligationsTableRows,
          ariaLabelledBy: "obligations-table-heading",
          attributes: { "aria-describedby": "obligations-tonnes-note" },
          headMaterialKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableMaterial'),
          headObligationToMeetKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableObligationToMeet'),
          headAwaitingKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableAwaiting'),
          headAcceptedKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableAccepted'),
          headOutstandingKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableOutstanding'),
          headStatusKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableStatus')
        }) }}
      </div>
    </section>

    <section class="govuk-grid-row" aria-labelledby="glass-table-heading">
      <div class="govuk-grid-column-full">
        <h2 id="glass-table-heading" class="govuk-heading-m">{{ componentT(locale, pageLocaleBase, 'obligationsTable', 'glassHeading') }}</h2>

        {{ appCertificateObligationsTable({
          locale: locale,
          tableRows: glassTableRows,
          ariaLabelledBy: "glass-table-heading",
          headMaterialKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableMaterial'),
          headObligationToMeetKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableObligationToMeet'),
          headAwaitingKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableAwaiting'),
          headAcceptedKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableAccepted'),
          headOutstandingKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableOutstanding'),
          headStatusKey: componentLocaleKey(locale, pageLocaleBase, 'obligationsTable', 'tableStatus')
        }) }}
      </div>
    </section>

    <section class="govuk-grid-row" aria-labelledby="declaration-heading">
      <div class="govuk-grid-column-two-thirds">
        <h2 id="declaration-heading" class="govuk-heading-m">{{ componentT(locale, declarationLocaleBase, 'declaration', 'heading') }}</h2>

        {{ appComplianceSubmitDeclarationText({
          locale: locale,
          localeBase: declarationLocaleBase,
          interpolation: { organisationName: organisationName }
        }) }}

        <p class="govuk-body">
          {{ componentT(locale, pageLocaleBase, 'page', 'verifiedByPrefix') }}
          <strong>{{ submitterName }}</strong>
        </p>
      </div>
    </section>

    <section class="govuk-grid-row">
      <div class="govuk-grid-column-full">
        <div class="govuk-button-group">
          {{ govukButton({
            text: componentT(locale, pageLocaleBase, 'page', 'returnButton'),
            href: eprPackaging.manageYourRecyclingObligationsUrl,
            classes: "govuk-button--secondary"
          }) }}
          <span class="govuk-body govuk-!-margin-bottom-0">
            <a class="govuk-link govuk-link--disabled" aria-disabled="true" tabindex="-1">{{ componentT(locale, pageLocaleBase, 'page', 'downloadLink') }}</a>
          </span>
        </div>
      </div>
    </section>
  </div>
{% endblock %}
