{% from "certificate-obligations-table/macro.njk" import appCertificateObligationsTable %}

{#
  Layout wrappers are plain divs (not labelled sections). The scrollable table
  containers already expose a unique region landmark via aria-labelledby —
  labelling both caused axe landmark-unique failures.
#}
{% macro appComplianceSubmitObligationsTables(params) %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      {% if params.obligationsAriaLabelledBy %}
        <h2 id="obligations-table-heading" class="govuk-visually-hidden">{{ params.i18n.ct('obligationsTable', 'recyclingObligationsHeading') }}</h2>
      {% else %}
        <h2 id="obligations-table-heading" class="govuk-heading-l">{{ params.i18n.ct('obligationsTable', 'recyclingObligationsHeading') }}</h2>
      {% endif %}
      <p id="obligations-tonnes-note" class="govuk-body govuk-!-margin-bottom-4">{{ params.i18n.ct('obligationsTable', 'tonnesNote') }}</p>

      {{ appCertificateObligationsTable({
        locale: params.locale,
        i18n: params.i18n,
        tableRows: params.obligationsTableRows,
        ariaLabelledBy: "obligations-table-heading",
        attributes: { "aria-describedby": "obligations-tonnes-note" }
      }) }}
    </div>
  </div>

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <h3 id="glass-table-heading" class="govuk-heading-m">{{ params.i18n.ct('obligationsTable', 'glassHeading') }}</h3>

      {{ appCertificateObligationsTable({
        locale: params.locale,
        i18n: params.i18n,
        tableRows: params.glassTableRows,
        ariaLabelledBy: "glass-table-heading",
        attributes: { "aria-describedby": "obligations-tonnes-note" }
      }) }}
    </div>
  </div>
{% endmacro %}
